function nodes = UpdateNode3(nodes,gridSize)

nodes=[nodes;(rand(1,2)-0.5)*gridSize]; %0.5*gridsize för att kunna ha pos och negativa värden
                                               % vill kunna hamna överallt
                                               % på grid

end
